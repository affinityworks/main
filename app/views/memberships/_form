<%= form_tag("memberships", method: "post") do %>

  <div class="field">
    <%= label_tag "Update Role" %>
    <%#collection_select does work, yet but is here to remind you that this is what it will look like %>
    <%# <%= collection_select(:current_group, :group_id, @membership.all, :person_id, :given_name, :family_name) %> %>
  </div>


  <div class="actions">
    <%= submit_tag("Change Role") %>
  </div>
<% end %>