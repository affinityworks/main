<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
     </head>
  <body>
    <%= form_tag("memberships", method: "post") do %>

      <div class="field">
        <%= label_tag "Add Organizer Permissions:" %>

        <%= collection_select(:person, :id, @current_members, :id, :name) %>
      </div>


      <div class="actions">
        <%= submit_tag("Add Permissions") %>
      </div>
    <% end %>
      

  </body>
</html>