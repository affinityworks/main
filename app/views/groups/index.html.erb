<p id="notice"><%= notice %></p>

<h1>Groups</h1>

<table>
  <thead>
    <tr>
      <th>Origin system</th>
      <th>Name</th>
      <th>Description</th>
      <th>Summary</th>
      <th>Browser url</th>
      <th>Featured image url</th>
      <th>Creator</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td><%= group.origin_system %></td>
        <td><%= group.name %></td>
        <td><%= group.description %></td>
        <td><%= group.summary %></td>
        <td><%= group.browser_url %></td>
        <td><%= group.featured_image_url %></td>
        <td><%= group.creator_id %></td>
        <td><%= link_to 'Show', group %></td>
        <td><%= link_to 'Edit', edit_group_path(group) %></td>
        <td><%= link_to 'Destroy', group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Group', new_group_path %>
